// Slick slider
// Route section
$(document).ready(function () {
  $('.route-slider').slick({
    // slidesToShow: ,
    dots: true,
    centerMode: true,
  });
});

// Gallery section
$(document).ready(function () {
  $('.gallery__wrapper').slick({
    // Кол-во фото для показа
    slidesToShow: 3,
    dots: true,
    centerMode: true,
  });
});
/* ---------------------------------------- */
