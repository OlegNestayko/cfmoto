// Slick slider
// Route section
$(document).ready(function () {
  $('.route-slider').slick({
    dots: true,

    centerMode: true,
  });
});

// Gallery section
$(document).ready(function () {
  $('.gallery__wrapper').slick({
    slidesToShow: 3,

    dots: true,

    centerMode: true,
  });
});
/* ---------------------------------------- */
